# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Env / secrets (keep the documented example)
.env
.env.*
!.env.example

# Project data (vector store, ingested artifacts)
.opendomain/
*.sqlite3
# Stray Chroma dir created when data_dir resolved to None
None/

# Runtime logs
*.log

# Node / frontend
node_modules/
web/dist/
web/test-results/
web/playwright-report/
src/opendomainmcp/api/static/

# Local debug screenshots at repo root (docs/screenshots/ stays tracked)
/*.png

# Generated media (brag / launch-video output)
brag-output/

# OS / editor
.DS_Store
.idea/
.vscode/

# Claude Code agent worktrees / local state (but track shared, checked-in skills)
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/run-open-domain-mcp/
.gstack/

# Subagent-driven-development scratch (briefs, reports, diffs, ledger)
.superpowers/
.worktrees/

# ERPNext benchmark corpus (re-fetched via setup_corpus.sh; do not commit GPL source)
benchmarks/erpnext/.corpus/
benchmarks/erpnext/*.report.json

# Playwright MCP debug dumps (local artifacts)
.playwright-mcp/
