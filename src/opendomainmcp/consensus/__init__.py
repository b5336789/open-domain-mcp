"""Consensus workflows for cross-validating domain knowledge.

Synthesizes rule units from chunks and chains, building a unified model of
validated business rules backed by multiple sources. The consensus layer
bridges retrieval (chunks + chains), extraction (evidence), and storage
(RuleItem).
"""
